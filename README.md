# Лабораторная работа №6
## Непрерывная интеграция и упаковка проектов с помощью CPack

**Студент:** Сардарян Гор ИУ8-22

## Цель работы
Настроить систему упаковки CPack для генерации установочных пакетов различных форматов (DEB, RPM, TGZ, ZIP) и автоматизировать создание релизов на GitHub с использованием GitHub Actions при пуше тегов.

## Выполненные шаги
1. Создан публичный репозиторий `lab06`. В него скопированы все необходимые библиотеки (`formatter_lib`, `formatter_ex_lib`, `solver_lib`, `solver_application`) из предыдущих лабораторных работ.
2. Доработан корневой файл `CMakeLists.txt`:
   - Добавлена установка исполняемого файла `solver` в `bin`.
   - Введена переменная `SOLVER_VERSION` (значение берётся из `version.txt` или задаётся вручную).
3. Создан файл `CPackConfig.cmake`, в котором указаны:
   - контактная информация и имя пакета;
   - генераторы пакетов (`DEB`, `RPM`, `TGZ`, `ZIP`); Windows: .msi.  macOS: .dmg.
   - специфичные для Debian и RPM настройки (maintainer, архитектура и т.д.);
   - список директорий, включаемых в исходные архивы.
4. Настроен GitHub Actions workflow (файл `.github/workflows/ci.yml`):
   - Триггер: `push` на теги, начинающиеся с `v*`.
   - Установка необходимых инструментов: `rpm-build` (для создания RPM-пакетов).
   - Конфигурация CMake с параметром `-DCPACK_GENERATOR="DEB;RPM;TGZ;ZIP"`.
   - Сборка пакетов командой `cpack`.
   - Создание релиза на GitHub с помощью действия `softprops/action-gh-release` и прикрепление к нему всех сгенерированных пакетов.
5. Исправлены ошибки:
   - В `CMakeLists.txt` добавлена явная зависимость `formatter_ex_lib` от `formatter_lib` (чтобы избежать проблем с линковкой).
   - Для Debian-пакета указан обязательный параметр `CPACK_DEBIAN_PACKAGE_MAINTAINER`.
6. Созданы теги `v1.0.0`, `v1.0.1`, `v1.0.2`, `v1.0.3`, `v1.0.4`, `v1.0.5`. Последний тег (`v1.1.3`) успешно запустил workflow и создал релиз.

## Результаты
- Релиз `v1.1.3` доступен по адресу:  
  [https://github.com/Gor-debug/lab06/releases/tag/v1.1.3](https://github.com/Gor-debug/lab06/releases/tag/v1.1.3)
- К релизу приложены четыре пакета: `.deb`, `.rpm`, `.tar.gz`, `.zip`. Также msi и dmg

## Вывод
В ходе выполнения лабораторной работы №6 я:

Настроил систему упаковки CPack для генерации установочных пакетов различных форматов (DEB, RPM, TGZ, ZIP, MSI, DMG) в зависимости от целевой платформы.

Обеспечил автоматическую сборку пакетов под три операционные системы: Linux (Ubuntu), Windows и macOS с помощью GitHub Actions. Для каждой платформы заданы свои генераторы CPack.

Реализовал публикацию пакетов на GitHub Releases при создании тега вида `v*`. Все артефакты (`.deb`, `.rpm`, `.msi`, `.dmg`, `.tar.gz`, `.zip`) автоматически прикрепляются к релизу.

Научился разделять сборку на отдельные независимые задания (jobs) в GitHub Actions, что позволило запускать их параллельно и ускорить процесс.

Добавил бейджи состояния сборки и покрытия кода в `README.md`, что даёт наглядную информацию о статусе проекта.

Полностью автоматизировал процесс создания релизов: достаточно отправить тег, и все пакеты будут собраны и загружены без ручного вмешательства.

Таким образом, цели работы достигнуты: система непрерывной интеграции (CI) настроена для кросс-платформенной сборки и упаковки приложения, релизы создаются автоматически, пакеты доступны для скачивания пользователями под их операционные системы.

